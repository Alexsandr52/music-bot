version: '3.8'

services:
  music-bot:
    build: .
    container_name: telegram-music-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./tracks:/app/tracks
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
